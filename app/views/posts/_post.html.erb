<tr id="post_<%= post.id %>">
  <td>
    <h4><%=h post.title %></h4></br>
    <%=h post.content %></br>
    <strong><%=h post.user.try(:name) || "匿名" %></strong> | <%= post.created_at.to_s(:db) %>
  </td>
</tr>